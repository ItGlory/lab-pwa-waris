#!/bin/sh

# WARIS Pre-commit Hook
# Runs lint-staged on staged files

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Check exit code
if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Please fix the errors and try again."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
